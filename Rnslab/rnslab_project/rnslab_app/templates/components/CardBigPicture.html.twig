<div{{ attributes }}>
        <div class="imgBigPicture {{ hook }}">
            <img src="../images/{{ mediaSrc }}" class="img-fluid" alt="{{ mediaAlt }}"
                style="width: 100%; height:80vh; object-position: 50% 50%; object-fit:cover">
            <div class="container textBigPicture">
                <div class="row">
                    <div class="col-md-6 col-sm-12">
                        <h2>{{ title }}</h>
                            <h3>{{ subtitle }}</h3>
                    </div>
                    <div class="col-md-6 clo-sm-12">
                        <p>{{ textContent|raw }}</p>
                    </div>
                </div>
            </div>
        </div>
</div>