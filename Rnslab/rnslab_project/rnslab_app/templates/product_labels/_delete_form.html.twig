<form method="post" action="{{ path('app_product_labels_delete', {'id': product_label.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_label.id) }}">
    <button class="btn">Delete</button>
</form>
